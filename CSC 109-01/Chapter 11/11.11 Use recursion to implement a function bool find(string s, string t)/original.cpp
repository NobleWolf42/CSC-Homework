/*
Author: Ben Carpenter
Copyright: 2024

Assignment:
Use recursion to implement a function bool find(string s, string t) that tests whether a string t is contained in a string s:

    *bool b = s.find("Mississippi!", "sip"); // Returns true
    *
    *Hint: If the text starts with the string you want to match, then you are done. If not, consider the sentence that you obtain by removing the first character.

*/
#include <iostream>
#include <string>

using namespace std;

/**
   Tests if a string is a subset of another.
   @param s a string
   @param t a string
   @return true if s contains t
*/
bool find(string s, string t) {  
   // TODO
   
   // Hint: Use substring, length and recursion 
}

int main() {
   
   // Create string variable(s)
   
   // Print Expected, "Enter the first string: "
   // Print Expected, "Enter the second string: "
   
   // Remember to allow users to input

   // Create another varible and set it equal to a function 

   /* 
   IF the string is found/correct then Print Expected, 
   "The substring " (print second string here) " was found in " (print first string here) ".\n" 
   */
   
   /* 
   If the string is NOT found/NOT correct then Print Expected, 
   "The substring " (print second string here) " was not found in " (print first string here) ".\n" 
   */

   return 0;
}
